```mermaid

graph TD
    User[User Browser] -->|1. DNS Query| DNS[DNS Resolver]
    DNS -->|2. Returns IP| User
    User -->|3. HTTP Request| HAProxy[HAProxy Load Balancer]

    subgraph Server_1 [Web App Server 1]
        Nginx1[Nginx Web Server]
        App1[Application Server]
        Code1[Codebase]
        DB_Master[(MySQL Primary)]
    end

    subgraph Server_2 [Web App Server 2]
        Nginx2[Nginx Web Server]
        App2[Application Server]
        Code2[Codebase]
        DB_Slave[(MySQL Replica)]
    end

    HAProxy -->|4a. Forward| Nginx1
    HAProxy -->|4b. Forward| Nginx2

    Nginx1 --> App1
    App1 --> Code1
    App1 --> DB_Master

    Nginx2 --> App2
    App2 --> Code2
    App2 --> DB_Slave

    DB_Master -->|5. Replication| DB_Slave
